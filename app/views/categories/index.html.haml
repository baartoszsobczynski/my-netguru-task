#index_category
  %h1 Categories

  - if current_user && current_user.admin?
    #new_category_button
      = link_to 'Add category', new_category_path, class: "btn btn-success "

  %table.table
    - categories.each do |category|
      %tr
        %td= "#{link_to category.name, category} <small>(#{pluralize(category.products.count, 'product')})</small>".html_safe
        - if current_user && current_user.admin?
          %td
            = link_to 'Edit', edit_category_path(category), class: "btn btn-default glyphicon glyphicon-edit"
          %td
            = link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-warning glyphicon glyphicon-fire"

-#%h1 Listing categories
-#
-#%table.table
-#  %tr
-#    %th Name
-#    %th
-#    %th
-#    %th
-#
-#  - categories.each do |category|
-#    %tr
-#      %td= link_to category.name, category
-#      - if current_user && current_user.admin?
-#        %td= link_to 'Edit', edit_category_path(category)
-#      - if current_user && current_user.admin?
-#        %td= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' }
-#
-#%br
-#
-#- if current_user && current_user.admin?
-#  = link_to 'New Category', new_category_path