#edit_category

  %h1 Editing category
  = link_to 'Show', category, class: 'btn btn-primary btn-lg glyphicon glyphicon-chevron-right'
  = link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-warning btn-lg glyphicon glyphicon-fire"
  = link_to 'Back', categories_path, class: 'btn btn-default btn-lg glyphicon glyphicon-chevron-left'

  #my-own-form
    = render 'form'
